{
    "name": "casio-emulator",
    "module": "index.ts",
    "scripts": {
        "build": "rm -rf ./build && bun build ./web/index.html --outdir ./build --target browser --chunk-naming=[name].[ext] --asset-naming=[name].[ext] --sourcemap=linked --minify"
    },
    "devDependencies": {
        "@types/bun": "^1.3.13"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "private": true,
    "type": "module",
    "dependencies": {
        "@types/decimal.js": "^7.4.3",
        "decimal.js": "^10.6.0"
    }
}